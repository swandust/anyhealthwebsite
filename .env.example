# Copy to .env and fill in to enable website analytics + contact form storage.
# The site works fine without these; tracking silently no-ops until they are set.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
